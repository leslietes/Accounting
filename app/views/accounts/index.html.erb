<h1>Accounts</h1>

<ul>
	<% @accounts.each do |account| %>
	<li>
		<%= account.name %> - <%= account._type %>
		<%= link_to 'Edit', edit_account_path(account) %>
		<%= link_to 'Delete', account_path(account), :method => :delete, :confirm => "Are you sure?" %>
	</li>
	<% end %>
</ul>	

<%= link_to 'New', new_account_url %>